{% extends 'base.html.twig' %}

{% block body %}
    <h1>Utilisateurs creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('badr_entr_commande') }}">Back to the produit list</a>
        </li>
    </ul>
{% endblock %}
